<h3>404 Page Not found</h3>
<!DOCTYPE html>
<html>
    <head>

    </head>
    <body>
        {{>header}}
        <h1>404 page hbs</h1>
        <h3>{{title}}, {{name}}</h3>
        {{>footer}}
    </body>
</html>